# Beet
Die Ordner müssen als libraries in Arduino IDE hinzugefügt werden.
im code müssen die verwendeten pins oben bei den Moisure32 objekten und unten in den getMoisture() methoden geändert werden, pin nummern stehen in der README datei
